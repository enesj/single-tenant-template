{:expenses
 {:display-defaults
    {:show-edit? true
   :show-filtering? true
   :show-pagination? true
   :show-add-button? true
   :show-highlights? true
   :show-timestamps? true}

  ;; Locked (policy) settings users cannot override.
  :display-locks
  {:show-select? false
   :show-delete? false
   :show-batch-edit? false
    :show-batch-delete? false}

  ;; Non-display option consumed by the domain (not by the display resolver yet).
  :per-page 25}}
