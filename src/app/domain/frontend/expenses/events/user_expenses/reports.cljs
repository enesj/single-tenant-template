(ns app.domain.frontend.expenses.events.user-expenses.reports
  "Compatibility facade — requires sub-namespaces to load all event registrations."
  (:require
    app.domain.frontend.expenses.events.user-expenses.reports.state
    app.domain.frontend.expenses.events.user-expenses.reports.fetch))
