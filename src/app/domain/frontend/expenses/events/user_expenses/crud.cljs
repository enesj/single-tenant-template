(ns app.domain.frontend.expenses.events.user-expenses.crud
  "Compatibility facade — requires sub-namespaces to load all event registrations."
  (:require
    app.domain.frontend.expenses.events.user-expenses.crud.bridges
    app.domain.frontend.expenses.events.user-expenses.crud.expenses))
