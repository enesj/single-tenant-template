(ns app.template.backend.utils.adapters.normalization
  "DEPRECATED (removed): do not use.

  This namespace previously re-exported normalization helpers.
  Call sites should use `app.shared.adapters.normalization` directly.")
