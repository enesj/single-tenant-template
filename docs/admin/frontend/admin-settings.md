<!-- ai: {:tags [:frontend :admin :settings] :kind :guide} -->

# Admin Settings (Single-Tenant)

This guide covers the unified Admin Settings UI.

- `/admin/admin-settings` — Admin panel configuration (admin scope)
- `/admin/user-settings` — Domain-owned user UI config (user-facing defaults/locks)

Use these routes during development and production.
