({:action :create-type,
  :type-name :invitation-status,
  :model-name :tenant_invitations,
  :options
  {:choices ["pending" "accepted" "expired" "revoked"], :type :enum}}
 {:action :create-type,
  :type-name :tenant-status,
  :model-name :tenants,
  :options {:choices ["active" "suspended" "archived"], :type :enum}}
 {:action :create-table,
  :model-name :tenants,
  :fields
  {:id {:primary-key true, :type :uuid},
   :name {:null false, :type [:varchar 255]},
   :slug {:null false, :type [:varchar 255]},
   :status
   {:null false, :default "active", :type [:enum :tenant-status]},
   :created-at {:default [:now], :type :timestamptz},
   :updated-at {:default [:now], :type :timestamptz}}}
 {:action :create-type,
  :type-name :invitation-role,
  :model-name :tenant_invitations,
  :options {:choices ["admin" "member" "viewer"], :type :enum}}
 {:action :create-table,
  :model-name :tenant_invitations,
  :fields
  {:role {:null false, :type [:enum :invitation-role]},
   :updated-at {:default [:now], :type :timestamptz},
   :email {:null false, :type :text},
   :expires-at {:null false, :type :timestamptz},
   :invited-by
   {:foreign-key :users/id,
    :null false,
    :on-delete :cascade,
    :type :uuid},
   :token {:null false, :type [:varchar 255]},
   :status
   {:null false, :default "pending", :type [:enum :invitation-status]},
   :id {:primary-key true, :type :uuid},
   :created-at {:default [:now], :type :timestamptz},
   :tenant-id
   {:foreign-key :tenants/id,
    :null false,
    :on-delete :cascade,
    :type :uuid}}}
 {:action :create-index,
  :index-name :idx-tenant-invitations-status,
  :model-name :tenant_invitations,
  :options {:fields [:status], :type :btree}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :receipts,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :create-index,
  :index-name :idx-receipts-tenant,
  :model-name :receipts,
  :options {:fields [:tenant_id], :type :btree}}
 {:action :create-type,
  :type-name :impersonation-role,
  :model-name :impersonation_grants,
  :options {:choices ["owner" "admin" "member" "viewer"], :type :enum}}
 {:action :create-type,
  :type-name :impersonation-status,
  :model-name :impersonation_grants,
  :options {:choices ["active" "revoked"], :type :enum}}
 {:action :create-table,
  :model-name :impersonation_grants,
  :fields
  {:role {:null false, :type [:enum :impersonation-role]},
   :updated-at {:default [:now], :type :timestamptz},
   :revoked-by-admin
   {:foreign-key :admins/id, :on-delete :set-null, :type :uuid},
   :admin-id
   {:foreign-key :admins/id,
    :null false,
    :on-delete :cascade,
    :type :uuid},
   :revoked-by-user
   {:foreign-key :users/id, :on-delete :set-null, :type :uuid},
   :status
   {:null false,
    :default "active",
    :type [:enum :impersonation-status]},
   :id {:primary-key true, :type :uuid},
   :granted-by
   {:foreign-key :users/id,
    :null false,
    :on-delete :cascade,
    :type :uuid},
   :created-at {:default [:now], :type :timestamptz},
   :tenant-id
   {:foreign-key :tenants/id,
    :null false,
    :on-delete :cascade,
    :type :uuid}}}
 {:action :create-index,
  :index-name :idx-impersonation-grants-status,
  :model-name :impersonation_grants,
  :options {:fields [:status], :type :btree}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :payer_types,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :create-type,
  :type-name :membership-role,
  :model-name :tenant_memberships,
  :options {:choices ["owner" "admin" "member" "viewer"], :type :enum}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :expense_categories,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :create-index,
  :index-name :idx-expense-categories-tenant-name,
  :model-name :expense_categories,
  :options {:fields [:tenant_id :name], :unique true, :type :btree}}
 {:action :create-type,
  :type-name :membership-status,
  :model-name :tenant_memberships,
  :options {:choices ["active" "suspended"], :type :enum}}
 {:action :create-table,
  :model-name :tenant_memberships,
  :fields
  {:id {:primary-key true, :type :uuid},
   :tenant-id
   {:foreign-key :tenants/id,
    :null false,
    :on-delete :cascade,
    :type :uuid},
   :user-id
   {:foreign-key :users/id,
    :null false,
    :on-delete :cascade,
    :type :uuid},
   :role {:null false, :type [:enum :membership-role]},
   :status
   {:null false, :default "active", :type [:enum :membership-status]},
   :invited-by
   {:foreign-key :users/id, :on-delete :set-null, :type :uuid},
   :created-at {:default [:now], :type :timestamptz},
   :updated-at {:default [:now], :type :timestamptz}}}
 {:action :create-index,
  :index-name :idx-tenant-memberships-user,
  :model-name :tenant_memberships,
  :options {:fields [:user_id], :type :btree}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :user_expense_settings,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :drop-table, :model-name :price_observations}
 {:action :create-index,
  :index-name :idx-tenant-memberships-status,
  :model-name :tenant_memberships,
  :options {:fields [:status], :type :btree}}
 {:action :create-index,
  :index-name :idx-tenants-status,
  :model-name :tenants,
  :options {:fields [:status], :type :btree}}
 {:action :alter-column,
  :field-name :user-id,
  :model-name :user_expense_settings,
  :options
  {:foreign-key :users/id,
   :null false,
   :on-delete :cascade,
   :type :uuid},
  :changes
  {:foreign-key {:from :EMPTY, :to :users/id},
   :null {:from :EMPTY, :to false},
   :on-delete {:from :EMPTY, :to :cascade},
   :primary-key {:from true, :to :EMPTY}}}
 {:action :create-index,
  :index-name :uniq-user-expense-settings-tenant-user,
  :model-name :user_expense_settings,
  :options {:fields [:tenant_id :user_id], :unique true, :type :btree}}
 {:action :add-column,
  :field-name :id,
  :model-name :user_expense_settings,
  :options {:primary-key true, :type :uuid}}
 {:action :create-table,
  :model-name :user_sessions,
  :fields
  {:id {:primary-key true, :type :uuid},
   :session-key {:null false, :type [:varchar 255]},
   :data {:null false, :type :jsonb},
   :expires-at {:null false, :type :timestamptz},
   :created-at {:default [:now], :type :timestamptz},
   :updated-at {:default [:now], :type :timestamptz}}}
 {:action :create-index,
  :index-name :idx-user-sessions-expires,
  :model-name :user_sessions,
  :options {:fields [:expires_at], :type :btree}}
 {:action :drop-index,
  :index-name :uniq-payer-types-default,
  :model-name :payer_types}
 {:action :drop-index,
  :index-name :idx-receipts-file-hash,
  :model-name :receipts}
 {:action :create-index,
  :index-name :idx-impersonation-grants-admin,
  :model-name :impersonation_grants,
  :options {:fields [:admin_id], :type :btree}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :expenses,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :create-index,
  :index-name :idx-expenses-tenant,
  :model-name :expenses,
  :options {:fields [:tenant_id], :type :btree}}
 {:action :create-index,
  :index-name :idx-tenants-slug,
  :model-name :tenants,
  :options {:fields [:slug], :unique true, :type :btree}}
 {:action :create-index,
  :index-name :idx-impersonation-grants-tenant,
  :model-name :impersonation_grants,
  :options {:fields [:tenant_id], :type :btree}}
 {:action :create-index,
  :index-name :idx-user-sessions-key,
  :model-name :user_sessions,
  :options {:fields [:session_key], :unique true, :type :btree}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :expense_items,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :create-index,
  :index-name :idx-expense-items-tenant,
  :model-name :expense_items,
  :options {:fields [:tenant_id], :type :btree}}
 {:action :add-column,
  :field-name :tenant-id,
  :model-name :payers,
  :options
  {:foreign-key :tenants/id,
   :null false,
   :on-delete :cascade,
   :type :uuid}}
 {:action :create-index,
  :index-name :idx-payers-tenant,
  :model-name :payers,
  :options {:fields [:tenant_id], :type :btree}}
 {:action :drop-index,
  :index-name :idx-payer-types-label,
  :model-name :payer_types}
 {:action :create-index,
  :index-name :idx-payer-types-tenant-label,
  :model-name :payer_types,
  :options {:fields [:tenant_id :label], :unique true, :type :btree}}
 {:action :create-index,
  :index-name :idx-tenant-invitations-tenant,
  :model-name :tenant_invitations,
  :options {:fields [:tenant_id], :type :btree}}
 {:action :create-index,
  :index-name :idx-receipts-tenant-file-hash,
  :model-name :receipts,
  :options
  {:fields [:tenant_id :file_hash], :unique true, :type :btree}}
 {:action :create-index,
  :index-name :uniq-tenant-memberships-tenant-user,
  :model-name :tenant_memberships,
  :options {:fields [:tenant_id :user_id], :unique true, :type :btree}}
 {:action :create-index,
  :index-name :idx-tenant-invitations-token,
  :model-name :tenant_invitations,
  :options {:fields [:token], :unique true, :type :btree}}
 {:action :create-index,
  :index-name :uniq-payer-types-tenant-default,
  :model-name :payer_types,
  :options
  {:fields [:tenant_id :is_default],
   :unique true,
   :where [:= :is_default true],
   :type :btree}}
 {:action :drop-index,
  :index-name :idx-expense-categories-name,
  :model-name :expense_categories}
 {:action :create-index,
  :index-name :idx-tenant-invitations-email,
  :model-name :tenant_invitations,
  :options {:fields [:email], :type :btree}})
