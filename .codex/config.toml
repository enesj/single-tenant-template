# Project-local Codex configuration for MCP
# Multi-tenant property hosting SaaS - Clojure/ClojureScript integration

# Model configuration (project-specific)
model = "gpt-5.4"
model_reasoning_effort = "high"
model_context_window = 500000
model_auto_compact_token_limit = 400000
