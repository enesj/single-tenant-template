Modify the Expense items table like this:
1. Remove the Payer label, Supplier ID, Supplier display name  columns.
2. Update all related code to align with these changes.
3. Raw label column should contain the Id from new Raw_labels table as FK.
4. When Receipt is exporting if there is same raw label in raw_labes table iseert its ID in this column, if not create the new raw_label in raw_labels table and use its id.


Rules for implementation:
1. Before starting migration read ALL migrations docs carefully.
2. Apply migrations to both DEV and TEST databases.
3. Use the clojure-mcp eval tools as first choice  for testing and dubugging modifications
4. Use the postgres mcp for all db operations
5. If you need to test frontend us the chrome mcp for interactive testing
6. Use the clojure-mcp scratch pad to make the plan and track the progres

When finished align the existing test files with change and add new if needed
Finally update the docs if needed
